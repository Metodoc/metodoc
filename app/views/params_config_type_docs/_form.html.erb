<%= form_for(params_config_type_doc) do |f| %>
  <% if params_config_type_doc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(params_config_type_doc.errors.count, "error") %> prohibited this params_config_type_doc from being saved:</h2>

      <ul>
      <% params_config_type_doc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
